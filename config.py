"""
配置模块 — 集中管理 Agent 的所有参数。

设计原则：所有「魔法数字」和「连接信息」都放这里，
其他模块只 import，不硬编码。改模型 / 改循环上限时只动这一个文件。
"""

# ============================================
# 模型连接（Ollama 本地）
# ============================================
BASE_URL = "http://localhost:11434/v1"   # Ollama 的 OpenAI 兼容端点
API_KEY = "ollama"                       # Ollama 本地无需真实 key，随便填
MODEL = "qwen2.5:7b"                     # Agent 主力模型（tool-use 更稳定）

# ============================================
# Agent 参数
# ============================================
MAX_ITER = 8        # ReAct 循环最大轮数（安全网，防止无限循环）
MAX_TOKENS = 1024   # 单次响应的最大 token 数
TEMPERATURE = 0.0   # 0 = 确定性输出，工具调用更稳定（不会随机乱选工具）

# ============================================
# 文件读取长度阈值
# ============================================

MAX_FILE_CHARS = 2000
